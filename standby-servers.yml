---
- hosts: standby-servers
  become: yes
  remote_user: centos
  tasks:
    - name: stop zabbix-server
      service:
        name: zabbix-server
        enabled: no
        state: stopped
      tags: [standby]
