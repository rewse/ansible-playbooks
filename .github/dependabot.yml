version: 2

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Matches the runtime supply-chain cooldown used by the playbooks.
    cooldown:
      default-days: 4  # zizmor: ignore[dependabot-cooldown] matches the 96-hour minimum package age
    groups:
      github-actions:
        patterns: ["*"]

  - package-ecosystem: pre-commit
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 4  # zizmor: ignore[dependabot-cooldown] matches the 96-hour minimum package age
    groups:
      pre-commit:
        patterns: ["*"]
