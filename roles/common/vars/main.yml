---
email: "{{vault_email}}"
relayhost:
  name: mail.so-net.ne.jp
  port: 587
so_net:
  username: "{{vault_so_net.username}}"
  password: "{{vault_so_net.password}}"
swap_device_name: xvdb
ssh:
  rewse_jp:
    username: "{{vault_ssh.rewse_jp.username}}"
    port: "{{vault_ssh.rewse_jp.port}}"
zabbix_release_url: http://repo.zabbix.com/zabbix/3.2/rhel/7/x86_64/zabbix-release-3.2-1.el7.noarch.rpm
zabbix_server_name: zabbix.rewse.jp
