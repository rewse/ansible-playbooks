---
aws:
  access_key_id: "{{vault_aws.access_key_id}}"
  secret_access_key: "{{vault_aws.secret_access_key}}"
relayhost:
  name: mail.so-net.ne.jp
  port: 587
so_net:
  username: "{{vault_so_net.username}}"
  password: "{{vault_so_net.password}}"
swap_device_name: xvdb
