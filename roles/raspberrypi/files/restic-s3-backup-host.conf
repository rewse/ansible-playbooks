[Service]
# The root filesystem of this host and nothing else. The volumes of the enclosure
# have repositories of their own, so the walk is kept from reaching them, and the
# exclusion list is written for a system backup.
Environment=BACKUP_SOURCE=/
Environment=BACKUP_ONE_FILE_SYSTEM=1
Environment=BACKUP_EXCLUDE_FILE=/etc/restic.exclude
