---
- name: "nas : Reload exports"
  ansible.builtin.command: exportfs -ra
  changed_when: true

- name: "nas : Reload systemd"
  ansible.builtin.systemd:
    daemon_reload: true

- name: "nas : Restart smbd"
  ansible.builtin.systemd:
    name: smbd
    state: restarted
