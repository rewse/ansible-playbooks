#!/bin/sh

curl -so /etc/mail/spamassassin/local.cf.new http://www.flcl.org/~yoh/user_prefs

retval=$?

if [ $retval -ne 0 ]; then
  echo "<ERROR> Failed to download [CODE=$retval]" >&2
  exit $retval
fi

mv -f /etc/mail/spamassassin/local.cf{.new,}
