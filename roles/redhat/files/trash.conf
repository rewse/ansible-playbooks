if $programname startswith "dhclient" then {
  -/var/log/trash.log
  & stop
}

if $programname startswith "postfix" then {
  :msg,contains,"Illegal address syntax" -/var/log/trash.log
  & stop
}

if $programname startswith "postfix" then {
  :msg,contains,"Name or service not known" -/var/log/trash.log
  & stop
}
