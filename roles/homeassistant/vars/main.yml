---
ifttt_token: "{{ vault_ifttt_token }}"
nfs_server: 192.168.0.100
latlng:
  home:
    lat: "{{ vault_latlng.home.lat }}"
    lng: "{{ vault_latlng.home.lng }}"
  office:
    lat: "{{ vault_latlng.office.lat }}"
    lng: "{{ vault_latlng.office.lng }}"
  s:
    name: "{{ vault_latlng.s.name }}"
    lat: "{{ vault_latlng.s.lat }}"
    lng: "{{ vault_latlng.s.lng }}"
mosquitto_password: "{{ vault_mosquitto_password }}"
remo_token: "{{ vault_remo_token }}"
switchbot_token: "{{ vault_switchbot_token }}"
