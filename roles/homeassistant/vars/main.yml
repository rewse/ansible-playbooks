---
alexa_client:
  id: "{{ vault_alexa_client.id }}"
  secret: "{{ vault_alexa_client.secret }}"
card_mod_version: 3.4.5
frosted_glass_version: 1.2.1
gmail:
  address: "{{ vault_gmail.address }}"
  password: "{{ vault_gmail.password }}"
homeassistant_token: "{{ vault_homeassistant_token }}"
latlng:
  home:
    lat: "{{ vault_latlng.home.lat }}"
    lng: "{{ vault_latlng.home.lng }}"
mini_graph_card_version: 0.13.0
mini_media_player_version: 1.16.9
mosquitto_password: "{{ vault_mosquitto_password }}"
postgres:
  username: "{{ vault_postgres.username }}"
  password: "{{ vault_postgres.password }}"
  hostname: rewse-pg.rewse.jp
  db: homeassistant
regza:
  username: "{{ vault_regza.username }}"
  password: "{{ vault_regza.password }}"
simple_thermostat_version: 2.5.0
simple_weather_card_version: 0.8.5
tuya_local_version: 2025.9.0
