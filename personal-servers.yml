---
- hosts: personal-servers
  become: yes
  remote_user: centos
  roles:
    - srv
    - httpd
    - phpmyadmin
    - postfix
    - mailman
    - zabbix/server

- include: personal-standby-servers.yml
